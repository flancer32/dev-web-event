/**
 * Plugin constants (hardcoded configuration) for shared code.
 */
export default class Dev_Shared_Defaults {
    NAME = '@flancer32/dev-web-event';

    constructor() {
        Object.freeze(this);
    }
}
